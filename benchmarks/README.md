# Benchmarks

Benchmark workloads should produce reproducible throughput, latency, error,
timeout, queue-latency, and execution-latency measurements.

Suggested dimensions:

- worker count
- concurrent clients
- payload size
- cold start vs warm start
- retry and worker crash recovery
