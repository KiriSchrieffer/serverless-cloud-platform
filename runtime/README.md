# Runtime

Runtime images execute user function packages inside constrained containers.

The MVP Python runtime expects a mounted package containing `main.py` and a
handler path such as `main.handler`.
